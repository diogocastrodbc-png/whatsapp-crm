DATABASE_URL="postgresql://user:password@host:5432/whatsapp_crm"
PORT=3001
CORS_ORIGIN="http://localhost:3000"
NODE_ENV="development"

# Z-API credentials (https://app.z-api.io)
ZAPI_BASE_URL="https://api.z-api.io"
ZAPI_INSTANCE_ID="your-instance-id"
ZAPI_TOKEN="your-token"
# Security Token — gerado em app.z-api.io → sua instância → Settings → Security
# Diferente do ZAPI_TOKEN (que vai na URL). Este vai no header Client-Token de cada requisição.
ZAPI_CLIENT_TOKEN="your-client-token"

# URL pública onde a Z-API entrega webhooks de mensagens recebidas
# Desenvolvimento: use ngrok (ex: https://abc123.ngrok.io)
# Produção: URL do Railway ou outro host (ex: https://meu-backend.railway.app)
WEBHOOK_BASE_URL="https://your-public-url.com"
